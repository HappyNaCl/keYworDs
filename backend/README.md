# keYworDs Backend

## Getting Started

- Create a `.env` file based on `.env.example`
- Install dependencies: `npm install`
- Run the seeding script: `npm run seed:words`
- Start the dev server: `npm run start:dev`
